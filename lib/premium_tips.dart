import 'package:flutter/material.dart';

class PremiumTips extends StatelessWidget {
  const PremiumTips({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
